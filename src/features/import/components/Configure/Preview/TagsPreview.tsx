import React from 'react';
import { ZetkinTag } from 'utils/types/zetkin';
interface TagPreviewProps {
  tags: ZetkinTag[];
}
const TagsPreview = ({ tags }: TagPreviewProps) => {
  return <div>TagsPreview</div>;
};

export default TagsPreview;
